# ============================================================
# Mailflow Agent 本地环境变量示例
# ------------------------------------------------------------
# 使用方式：
# 1. 复制本文件为 `.env`；
# 2. 填入真实密钥；
# 3. `.env` 已被 .gitignore 忽略，不要提交。
# ============================================================

# 当前运行环境。日常本地开发使用 development。
APP_ENV=development

# 后端服务地址。Google OAuth 生成回调 URL 时会用到。
APP_BASE_URL=http://localhost:8000

# 前端 Vite 开发服务器地址。后端 CORS 只允许这个来源访问。
FRONTEND_BASE_URL=http://localhost:5173

# 应用主数据库。
# 推荐保持注释状态，让后端自动使用项目根目录 data/runtime/app.sqlite3。
# 如果手动填写相对路径，路径会受启动命令所在目录影响，容易生成到错误位置。
# DATABASE_URL=sqlite+aiosqlite:///./data/runtime/app.sqlite3

# LangGraph checkpoint 数据库路径。
# 推荐保持注释状态，让后端自动使用项目根目录 data/runtime/langgraph.sqlite3。
# LANGGRAPH_DB_PATH=./data/runtime/langgraph.sqlite3

# OAuth Token 加密 key。必须是 Fernet key。
# 可以用下面命令生成：
# .\.venv\Scripts\python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=replace-me

# Google OAuth Client ID。
# Gmail/Calendar 登录和授权时必须填写。即使只有你一个人使用，也需要它来完成 Google OAuth。
GOOGLE_CLIENT_ID=replace-me

# Google OAuth Client Secret。
# 只允许后端读取，不能写进前端代码、README、开发文档或聊天记录。
GOOGLE_CLIENT_SECRET=replace-me

# Google OAuth 回调地址。
# 必须和 Google Cloud Console 的 Authorized redirect URI 完全一致。
GOOGLE_REDIRECT_URI=http://localhost:8000/gmail/auth/callback

# Google OAuth 权限范围。
# openid/email/profile 用于确认当前授权的 Google 邮箱。
# Gmail/Calendar scope 用于读取、发送邮件和创建日程。
GOOGLE_OAUTH_SCOPES=openid email profile https://www.googleapis.com/auth/gmail.modify https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/calendar.freebusy

# 大模型提供商。当前项目默认使用 DeepSeek。
LLM_PROVIDER=deepseek

# DeepSeek 模型名。你指定采用 deepseek-v4-flash。
LLM_MODEL=deepseek-v4-flash

# DeepSeek API Base URL。
LLM_BASE_URL=https://api.deepseek.com

# DeepSeek API Key。
# 把真实 key 填在本地 `.env`，不要提交，也不要写进任何文档。
LLM_API_KEY=replace-me

# 大模型单次请求超时时间，单位秒。
# 防止 DeepSeek 或网络偶发无响应时，聊天页一直等待。
LLM_TIMEOUT_SECONDS=45
